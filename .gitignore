# Nuxt build / dev
.nuxt
.output
.netlify
dist
node_modules
.cache

# Env (keep example, never commit secrets)
.env
.env.local
.env.*.local

# Supabase
supabase/.branches
supabase/.temp

# OS
.DS_Store
Thumbs.db

# Bun
bun.lockb

# Local-only design references / handoff (not part of the app)
resources for claude/

# Seed image assets — large binaries live in a private Supabase bucket, not git.
# The manifests (targets.csv = upload list, provenance.csv = attribution) stay tracked.
seed/images/
seed/candidates/
seed/.cache/
