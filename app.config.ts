export default defineAppConfig({
  ui: {
    colors: {
      primary: 'primary',
      neutral: 'neutral',
    },
    button: {
      defaultVariants: {
        color: 'primary',
      },
    },
  },
})
